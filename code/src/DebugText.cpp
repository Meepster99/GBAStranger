
#include "DebugText.h"

#include "Game.h"

void DebugText::updateText() {
	
	
	
	//BN_LOG(frame);
	
	return;
	
	// this func is somehow so bad, that it causes frame drops.
	// im not sure what tf is happening, but please fix this.
	// sprite_text_generator essentially requires to regen all sprites and string 
	// every time i want to update it.
	// this is quite bad. is the on screen debug output(vs just logging) rlly worth it?
		
}