//! Build script to generate the rust code for the rooms. 

use std::io;

fn main() -> io::Result<()> {
    // TODO: generate rust code for rooms from JSON. 

    // If no errors, return OK. 
    return Ok(());
}
